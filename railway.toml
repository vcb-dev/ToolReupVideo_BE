# Railway chạy image Docker Hub (không build từ GitHub).
# Source Image: viejhaf/toolreup-video-be:latest

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Healthcheck trên Railway Dashboard: Path = /health
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
